def stringToNumber(str):
    return int(str)

print(stringToNumber("1500"))

#LAMBDA FUNCTION
#you don't have to use RETURN with lambda functions
textToNumber = lambda str: int(str)

print(textToNumber("123"))